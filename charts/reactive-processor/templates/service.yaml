apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactive-processor.fullname" . }}
  labels:
    {{- include "reactive-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactive-processor.selectorLabels" . | nindent 4 }}
