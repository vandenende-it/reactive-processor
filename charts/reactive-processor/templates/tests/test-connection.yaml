apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reactive-processor.fullname" . }}-test-connection"
  labels:
    {{- include "reactive-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reactive-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
